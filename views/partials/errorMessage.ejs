<!-- In this partial we render the an error message -->
<!-- IF there is an erroMessage variable being passed in we display the errorMessage -->
<% if (locals.errorMessage != null) { %> <%= errorMessage %> <% } %>
